part of 'theme.dart';

/// Spacing values for the design system.
class Spacing {
  static const _unit = 16.0;

  /// Spacing value 1pt.
  static const xxxs = 0.0625 * _unit;

  /// Spacing value 2pt.
  static const xxs = 0.125 * _unit;

  /// Spacing value 4pt.
  static const xs = 0.25 * _unit;

  /// Spacing value 8pt.
  static const sm = 0.5 * _unit;

  /// Spacing value 12pt.
  static const md = 0.75 * _unit;

  /// Spacing value 16pt.
  static const lg = 1 * _unit;

  /// Spacing value 24pt.
  static const xl = 1.5 * _unit;

  /// Spacing value 32pt.
  static const xxl = 2 * _unit;

  /// Spacing value 64pt.
  static const xxxl = 4 * _unit;

  const Spacing._();
}
